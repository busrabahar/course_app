<h1>Students Index</h1>
<%= button_to "Add New Student", new_student_path, methot: :get %>
<ul>
	<% @students.each do |student| %>
		<li><%= link_to student.name, student_path(student) %></li>
	<% end %>
</ul>
<!-- For each student instance, we are creating a link_to that directs us to the student instance’s show page -->